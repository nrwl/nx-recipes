{
  "name": "demo",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{projectRoot}/.output",
          "{projectRoot}/.nuxt"
        ]
      }
    },
    "tags": [
      "scope:demo"
    ],
    "implicitDependencies": [
      "tag:scope:demo"
    ]
  }
}
